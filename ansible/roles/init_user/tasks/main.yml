# Set user

- name: set up user
  user:
    name: "{{ user }}"
    state: present

- name: set up /etc/sudoers.d/{{ user }}.sudoers
  template: 
    src: sudoers
    dest: /etc/sudoers.d/{{ user }}
    mode: 0440
